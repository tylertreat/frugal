namespace go vendor_namespace (vendor="github.com/Workiva/some/vendored/place/vendor_namespace")
namespace dart vendor_namespace (vendor="some_vendored_place")
namespace java vendor_namespace.java (vendor="some.vendored.pkg")

const i32 a_const = 1

struct Item {}

enum MyEnum {
    ZERO,
    TWO = 2
}

service VendoredBase {}
