name: frugal
version: 0.0.1

environment:
  sdk: ^1.13.0

dependencies:
  logging: ^0.11.2
  messaging_frontend:
    git:
      url: git@github.com:Workiva/messaging-frontend-dart.git
      ref: 0.0.1
  thrift:
    hosted:
      name: thrift
      url: https://pub.workiva.org
    version: ^0.0.1
  uuid: any

dev_dependencies:
  test: ^0.12.6
